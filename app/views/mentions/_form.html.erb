<%= form_with(model: mention) do |form| %>
  <% if mention.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mention.errors.count, "error") %> prohibited this mention from being saved:</h2>

      <ul>
        <% mention.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :message_id, style: "display: block" %>
    <%= form.text_field :message_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
