<div class="navbar glass shadow-sm">
  <div class="navbar-start">
    <%# TODO Populate accordingly %>
    <div class="stat-value" data-controller="clock">
      <span data-clock-target="display">00:00:00</span>
    </div>
  </div>
  <div class="navbar-center hidden lg:flex">
    <ul class="menu menu-horizontal px-1">
      <li>
        <% if Current.user.authorised? %>
          <%= link_to "Erudite", admin_root_path, class: "btn btn-ghost text-xl", id: "test" %>
        <% else %>
          <%= link_to "Erudite", root_path, class: "btn btn-ghost text-xl", id: "test" %>
        <% end %>
      </li>
    </ul>
  </div>
  <div class="navbar-end">
    <% if Current.user.account_type == "teacher" %>
    <%= link_to "Available Courses",
                admin_root_path,
                class: "btn mx-2" %>
      <% end %>
    <%= link_to "Notifications",
                notifications_path,
                data: {
                  turbo_frame: "notifications",
                },
                class: "btn mx-2" %>

    <%= button_to "Sign Out", session_path, method: :delete, class: "btn mx-2" %>

  </div>
</div>