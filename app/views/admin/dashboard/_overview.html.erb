<turbo-frame id="tab_content">
  <div class="grid grid-cols-2 h-screen  outline-black">
    <div class="col col-span-2 overflow-auto p-4 border border-red-500">
      <%=link_to "create",
                 new_admin_user_path,
                 class: "btn btn-primary",
                 data: { turbo: "false" }%>
      <button class="btn btn-primary">Primary</button>
      <button class="btn btn-primary">Primary</button>
      <button class="btn btn-primary">Primary</button>
    </div>
    <div class="col overflow-auto p-4">
      <%= render 'admin/users/index' %>
    </div>
    <div class="col overflow-auto p-4">
      <%= render 'admin/courses/index' %>
    </div>
  </div>
</turbo-frame>