<div class="message">
  <p class="text-2xl"><%= message.title %></p>
  <%= simple_format(message.content) %> %>>
  <small>Posted by <%= message.user&.full_name || "Unknown" %> on <%= message.created_at.strftime("%B %d, %Y") %></small>
</div>